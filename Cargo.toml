[workspace]
resolver = "2"
members = [
    "pedalkernel",
    "pedalkernel-layout",
    "pedalkernel-validate",
    "xtask",
]

[profile.release]
lto = "thin"
strip = "symbols"

[profile.bench]
debug = true

[profile.profiling]
inherits = "release"
debug = 2
strip = "none"
