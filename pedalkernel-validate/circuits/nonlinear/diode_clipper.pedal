# Symmetric silicon diode clipper
# Topology matches SPICE: C1 (100n) → R1 (2.2k) → diodes → RL (10k)
# Clips at ±0.6V (typical silicon Vf)

pedal "Diode Clipper" {
  components {
    C1: cap(100n)       # Input coupling capacitor
    R1: resistor(2.2k)  # Series resistor (matches SPICE)
    D1: diode_pair(silicon)
    RL: resistor(10k)   # Load resistor
  }
  nets {
    in -> C1.a
    C1.b -> R1.a
    R1.b -> D1.a, RL.a
    D1.b -> gnd
    RL.b -> out
  }
}
